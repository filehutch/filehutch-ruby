# Omakase Ruby styling, same as the FileHutch app.
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

AllCops:
  Exclude:
    - "test/dummy/tmp/**/*"
    - "test/dummy/log/**/*"
    - "vendor/**/*"
