# Omakase Ruby styling, same as the AssetHutch app.
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

AllCops:
  Exclude:
    - "test/dummy/tmp/**/*"
    - "test/dummy/log/**/*"
    - "vendor/**/*"
