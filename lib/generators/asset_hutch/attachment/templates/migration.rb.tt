class <%= migration_class_name %> < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def change
    # Holds an AssetHutch file id ("file_…"). Never a URL, never a storage key.
    add_column :<%= table_name %>, :<%= column_name %>, :string
  end
end
