# Changelog

## 0.1.0 — unreleased

- `Assethutch::Client`: project, uploads (create, complete, one-call `upload`), files, signed URLs, delete.
- Typed errors mapped from the API's `error.code`.
- Rails: `has_assethutch_file` macro, `Assethutch::Engine` direct-upload endpoints, Stimulus controller,
  `assethutch:install` and `assethutch:attachment` generators.
