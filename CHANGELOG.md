# Changelog

## 0.1.0 — unreleased

- `Assetboar::Client`: project, uploads (create, complete, one-call `upload`), files, signed URLs, delete.
- Typed errors mapped from the API's `error.code`.
- Rails: `has_assetboar_file` macro, `Assetboar::Engine` direct-upload endpoints, Stimulus controller,
  `assetboar:install` and `assetboar:attachment` generators.
